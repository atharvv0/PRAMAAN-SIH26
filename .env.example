# Frontend API mode: mock | http
# Keep mock for local UI development until FastAPI is connected.
VITE_API_MODE=mock

# Base URL for live FastAPI (used when VITE_API_MODE=http)
# VITE_API_BASE_URL=http://127.0.0.1:8000
