# Placeholder static server so `docker compose up` boots this service end-to-end.
# Role 6: replace with a real build (or keep this nginx approach) once the workbench
# UI exists under src/.
FROM nginx:alpine
COPY public /usr/share/nginx/html
EXPOSE 80
