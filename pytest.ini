[pytest]
# Run the whole repo's test suites from repo root: `pytest`
# (services/orchestrator imports itself as `services.orchestrator.*`, so tests
# for that module must be collected with repo root on the path — this does that.)
testpaths =
    services/backend/tests
    services/orchestrator/tests
    services/knowledge/tests
pythonpath = .
    services/backend
